---
description: 项目核心代码规范与技术栈定义
globs: [“**/*.ts", "**/*.tsx", "**/*.js", "**/*.jsx", "**/*.json”]
alwaysApply: false
---

## 1.  核心身份与技术栈

你是一位资深前端工程师，正在开发一个现代化的 Web+Agent 应用。

### 核心原则：
- 编写整洁、不额外兜底的代码，禁止冗余
- 禁止使用废弃 API 
- 优先考虑代码的可读性和长期可维护性
- 

### 强制技术栈 (STRICT)：
- 框架: React 18+ (使用函数组件)
- 语言: TypeScript (启用严格模式 `strict: true`)
- 样式: Tailwind CSSv7
- 状态管理: 优先使用 React Context + Hooks 或 Zustand
- 包管理: npm 
- 图表与 UI：
  - Recharts（曲线图、热力图）
  - shadcn/ui（按钮、滑块、面板等）
- agent模型调用:Ollama
- HTTP 请求: 待补充

## 2. 禁止模式 (FORBIDDEN PATTERNS)

**绝对禁止使用以下任何模式：**
- 不要使用类组件
- 禁止使用 `any` 类型
- 禁止使用 var 声明变量
- 不要使用内联 style 对象 (使用 Tailwind 工具类)
- 避免不必要的 `useEffect`，优先使用事件处理函数

## 3.项目结构约定

严格遵守以下目录结构：
src/
├── components/    # 通用UI组件
├── hooks/         # 自定义 React Hooks
├── lib/           # 工具函数、第三方库配置
├── types/         # TypeScript 全局类型声明
├── pages/         # 页面级组件
├── utils/         # 全局通用函数
├── types/         # 需要共享的全局类型
├── router/        # 路由
└── stores/        # Zustand 状态管理

## 4. 命名规范

遵循以下命名约定，确保代码的一致性和可读性：
- 变量/函数: 使用 `camelCase`
- React 组件: 使用 `PascalCase`
- 常量: 使用 `SCREAMING_SNAKE_CASE`
- 文件夹: 使用 `kebab-case` (例如: `user-profile/`)
- 类型/接口: 使用 `PascalCase`
- 文件命名: 使用 `kebab-case` (例如: `user-service.ts`)

## 5.编码标准与风格

- 使用函数式编程风格，尽可能避免副作用
- 优先使用 `const` 而非 `let`，严禁使用 `var`
- 使用模板字符串进行字符串拼接
- 使用箭头函数
- 使用解构赋值 (props, state)
- 使用可选链 `?.` 和空值合并 `??
- 组件文件结构: (1) imports (2) types (3) constants (4) component implementation (5) export default
- Props 必须使用 `interface` 定义类型

## 6. 测试规范

- 单元测试使用 Vitest + React Testing Library
- 测试文件与源文件同目录，命名为 `*.test.tsx`
- 测试场景应覆盖组件的关键交互和渲染状态

## 7.文档与注释

- 所有公共函数和复杂 Hook 必须包含 JSDoc 注释，说明其用途、参数和返回值
- 组件注释应解释其整体功能，而非逐行解释代码
- **优先让代码本身具备可读性**：通过清晰的命名和结构来减少不必要的注释

## 8. 国际化

- 项目中所有面向用户的文本必须使用国际化库 (如 i18next 或 react-i18next)
- 不要在组件中硬编码文本字符串